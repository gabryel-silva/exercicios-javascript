const prompt = require('readline-sync');

//* funções de alta ordem
//? É um tipo de funcção que passa outra função como parâmetro

/*
function welcome(courseName) { 
    return (studentName) => {
        console.log(
            `Olá, ${studentName}! Seja bem vindo o  curso de ${courseName}`
            );
    }
}

const displayWelcomeToFrontEndCourse = welcome('Front end em React');

displayWelcomeToFrontEndCourse('Gabryel Silva');
*/
/*
const somar = (num1, num2) => num1 + num2;
const subtrair = (num1, num2) => num1 + num2;
const multiplicar = (num1, num2) => num1 + num2;
const dividir = (num1, num2) => num1 / num2;

const calcular = (num1, num2, operacao) => operacao(num1, num2);

const resultado = calcular(10, 5, somar);

console.log(resultado);
*/

/*
const numeros = [15,21,455,78,5]

numeros.forEach((item, index, arrayCompleto) => {
    console.log(index, item, arrayCompleto);
})
*/

